<?xml version="1.0" encoding="utf-8"?>
<!--
  Google Sign-In configuration.

  ┌──────────────────────────────────────────────────────────────────────────┐
  │ REQUIRED SETUP — sign-in will NOT work until you do this:                   │
  │                                                                            │
  │ 1. Go to https://console.cloud.google.com/ and create (or pick) a project. │
  │ 2. APIs & Services → Credentials → Create Credentials → OAuth client ID.   │
  │ 3. Create an **Android** client:                                           │
  │      - Package name: sukun.minimalist.app.launcher.com                      │
  │        (debug builds use sukun.minimalist.app.launcher.com.debug)           │
  │      - SHA-1 fingerprint of your signing key. Get it with:                  │
  │          ./gradlew signingReport                                            │
  │ 4. Also create a **Web application** OAuth client ID in the same project.   │
  │ 5. Paste that *Web* client ID (ends in .apps.googleusercontent.com) below.  │
  │    Credential Manager / Google ID requires the WEB client id as the        │
  │    server client id — NOT the Android one.                                  │
  │ 6. Configure the OAuth consent screen (add your account as a test user      │
  │    while the app is unpublished).                                           │
  └──────────────────────────────────────────────────────────────────────────┘
-->
<resources>
    <!-- TODO: replace with your OAuth 2.0 *Web application* client ID. -->
    <string name="default_web_client_id" translatable="false">YOUR_WEB_CLIENT_ID.apps.googleusercontent.com</string>
</resources>
